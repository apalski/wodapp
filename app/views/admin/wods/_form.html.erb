
	<%= f.label :title %>
	<%= f.text_field :title %><br><br>
	<%= f.label :wod_type %>
	<%= f.select :wod_type, ["weight", "time", "repetitions"], :prompt => "Select One" %><br><br>
	<%= f.label :description %>
	<%= f.text_field :description %>
	<%= f.collection_check_boxes :movement_ids, Movement.all, :id, :name %><br>