
	<div class="field" >
		<%= f.label :title %>
		<%= f.text_field :title %><br><br>
	</div>
	<div class="field" >	
		<%= f.label :wod_type %>
		<%= f.select :wod_type, ["weight", "time", "repetitions"], :prompt => "Select One" %><br><br>
	</div>
	<div class="field" >	
		<%= f.label :description %>
		<%= f.text_field :description %><br><br>
	</div>
	<%= f.collection_check_boxes :movement_ids, @movements, :id, :name %><br>