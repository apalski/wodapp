
<% if target.errors.any? %>
	<div id="error_explanation" >
		<h2><%= pluralize(target.errors.count, "error") %>
			did not allow this record to be saved: </h2>

		<ul>
			<% target.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>		
