
<% if target.errors.any? %>
	<div id="error_explanation" >
		<h2>Please correct the following <%= pluralize(target.errors.count, "error") %>:</h2>

		<ul>
			<% target.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>		
