

	<%= form_for @movement do |f| %>
		<% if @user.admin? %>
			<%= f.label :name %>
			<%= f.text_field :name %><br>
			<%= f.label :date %>
			<%= date_select :movement, :date %><br>
			<%= f.label :movement_type %>
			<%= f.select :movement_type, ["time", "weight", "repetitions"], :prompt => "Select One" %><br>
		<% end %>	
		<% if @user.user? %>
			<%= f.label :result %>
			<%= f.text_field :result %><br>
			<%= f.label :pr %>
			<%= f.check_box :pr %><br>
			<%= f.submit %>	
		<% end %>	
	<% end %>	
	
