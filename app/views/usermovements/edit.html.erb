

<%= form_for @usermovement do |f| %>
	<%= render 'form', f: f %>
	<%= f.submit "Update movement" %>	
<% end %>	
		
<%= link_to "Delete", user_usermovement_path(current_user, @usermovement), method: :delete %> 	