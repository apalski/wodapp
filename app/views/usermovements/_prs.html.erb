
<h1>  These are your current PRs</h1>
	<ul>
		<% moves = current_user.usermovements.by_pr %>
		<% moves.each do |move| %>
			<li><%= move.name %></li>
			<li><%= move.date.strftime("%d %B %Y") %></li>
			<li><%= move.result %></li>
			________________________
		<% end %>	
	</ul>	
